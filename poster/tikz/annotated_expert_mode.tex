% Annotated screenshot of expert mode (3D viewer + MSA panel).
%
% Replace figures/expert_mode.png with your actual screenshot. The TikZ
% overlay coordinates use the image's south-west corner as (0,0).
% Adjust the (x,y) of each callout once you can see your real image.
%
% Recommended screenshot:
%   - Expert mode on a tubulin structure with 2-3 chains aligned
%   - Nightingale MSA visible at the bottom with aux rows (variants /
%     ligand site / PTM) toggled on
%   - One residue highlighted in both 3D and MSA (e.g. GTP site)

\begin{tikzpicture}[remember picture]

  \node[anchor=south west, inner sep=0] (img)
    {\fbox{\parbox{0.95\linewidth}{
       \vspace{2.3cm}
       \centering\textcolor{tubMuted}{\textit{figures/expert\_mode.png}\\[4pt]
       \footnotesize 3D viewer (top) + Nightingale MSA (bottom),\\
       residue highlighted in both views, aux rows on}
       \vspace{2.3cm}
    }}};

  % Image dimensions for placing labels. Width is 0.95\linewidth; height
  % depends on the actual image. Treat the rectangle as 18cm wide for
  % overlay coords below -- adjust once you swap the real image in.

  % Common callout style (proper text wrapping inside fixed-width nodes)
  \tikzset{
    callout/.style={
      font=\footnotesize\sffamily, fill=white, fill opacity=0.92,
      text opacity=1, rounded corners=2pt, inner sep=3pt,
      anchor=west, align=left, text width=3.3cm
    },
  }

  % --- Callout 1: 1D <-> 3D sync
  \node[callout, draw=tubAccent]
    (c1) at ([xshift=-3.6cm, yshift=4.5cm]img.south east)
    {\textbf{1D\,$\leftrightarrow$\,3D sync} \\
     \scriptsize hover any residue in either view, the other follows};
  \draw[tubAccent, line width=0.7pt, ->, >=Stealth]
    (c1.west) -- ++(-2.2cm, -0.4cm);

  % --- Callout 2: multi-chain alignment
  \node[callout, draw=tubBeta]
    (c2) at ([xshift=-3.6cm, yshift=2.6cm]img.south east)
    {\textbf{multi-chain align} \\
     \scriptsize several PDB chains lifted onto the family master};
  \draw[tubBeta, line width=0.7pt, ->, >=Stealth]
    (c2.west) -- ++(-2.0cm, 0.3cm);

  % --- Callout 3: aux annotation rows
  \node[callout, draw=red!70]
    (c3) at ([xshift=-3.6cm, yshift=0.8cm]img.south east)
    {\textbf{aux annotation rows} \\
     \scriptsize variants, PTMs and ligand sites painted on the MSA};
  \draw[red!70, line width=0.7pt, ->, >=Stealth]
    (c3.west) -- ++(-1.8cm, 0.5cm);

\end{tikzpicture}
