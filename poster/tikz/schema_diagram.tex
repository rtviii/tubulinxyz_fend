% Simplified Neo4j graph schema.
% Embedded inside a \block in poster.tex.

\begin{tikzpicture}[
    every node/.style={font=\footnotesize\sffamily, align=center, rounded corners=2pt, inner sep=4pt},
    nstruct/.style={draw=tubAccent, fill=tubAccent!12, line width=0.8pt},
    nent/.style   ={draw=tubBeta,  fill=tubBeta!12,   line width=0.8pt},
    ninst/.style  ={draw=tubEpsilon!80!black, fill=tubEpsilon!18, line width=0.8pt},
    nchem/.style  ={draw=tubDelta, fill=tubDelta!12,  line width=0.8pt},
    nanno/.style  ={draw=red!70,   fill=red!8,        line width=0.8pt},
    ntax/.style   ={draw=tubGamma, fill=tubGamma!12,  line width=0.8pt},
    rel/.style    ={->, >=Stealth, gray!70, line width=0.6pt, font=\scriptsize\sffamily},
  ]

  \node[nstruct] (S)   at (0,0)       {\textbf{Structure}\\\scriptsize rcsb\_id, resolution, exp\_method};

  \node[nent]    (PE)  at (-4.8,-2.4) {\textbf{PolypeptideEntity}\\\scriptsize family, isotype, uniprot};
  \node[nent]    (NE)  at ( 0,  -2.4) {\textbf{NonpolymerEntity}\\\scriptsize entity\_id, chem\_id};
  \node[ntax]    (PHY) at ( 4.6,-2.4) {\textbf{PhylogenyNode}\\\scriptsize ncbi\_tax\_id};

  \node[ninst]   (PI)  at (-7.4,-4.6) {\textbf{PolypeptideInstance}\\\scriptsize auth\_asym\_id};
  \node[nanno]   (V)   at (-4.0,-4.6) {\textbf{Variant}\\\scriptsize master\_index, source};
  \node[nanno]   (M)   at (-1.0,-4.6) {\textbf{Modification}\\\scriptsize master\_index, mod\_type};
  \node[ninst]   (NI)  at ( 1.8,-4.6) {\textbf{NonpolymerInstance}};
  \node[nchem]   (C)   at ( 4.8,-4.6) {\textbf{Chemical}\\\scriptsize SMILES, DrugBank};

  \draw[rel] (S) -- node[above left] {DEFINES} (PE);
  \draw[rel] (S) -- node[right]      {DEFINES} (NE);
  \draw[rel] (S) -- node[above right]{LINEAGE} (PHY);

  \draw[rel] (PE) -- node[left]      {HAS\_VARIANT} (V);
  \draw[rel] (PE) -- node[right]     {HAS\_MOD}     (M);
  \draw[rel] (PI) -- node[left]      {INSTANCE\_OF} (PE);

  \draw[rel] (NI) -- node[right]     {INSTANCE\_OF} (NE);
  \draw[rel] (NE) -- node[right]     {DEFINED\_BY}  (C);

  \draw[rel, dashed, red!60] (C.west) to[bend left=20] node[below, font=\scriptsize\sffamily, text=red!70] {NEAR\_POLYMER} (PI.east);

\end{tikzpicture}

\vspace{2pt}
{\footnotesize\textit{The join key is \texttt{Variant.master\_index} (indexed).
Every variant, modification and ligand-contact resolves to the same residue across
the dataset.}}
