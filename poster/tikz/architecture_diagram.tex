% Pipeline architecture: compact, layered, "industrial software" style.
% Four grouped containers (Sources, Process, Store, Serve), each holding
% small white sub-cards with hairline borders. Subtle background tint per
% group; flow arrows are thin, dark grey, no shadows. Designed to live
% inside a half-width \block.
%
% Embedded inside a \block in poster.tex.

\begin{tikzpicture}[
    every node/.style={font=\sffamily, inner sep=0pt},
    grp/.style={
      draw=tubHairline2, line width=0.4pt, rounded corners=3pt,
      fill=tubSurface2, minimum height=42mm
    },
    grphead/.style={
      font=\condensedfont\fontsize{8}{10}\selectfont,
      text=tubMuted, anchor=south west
    },
    card/.style={
      draw=tubHairline, line width=0.3pt, rounded corners=2pt,
      fill=tubSurface, inner xsep=4pt, inner ysep=3pt,
      font=\fontsize{8}{10}\selectfont\sffamily,
      text=tubInk2, align=left, anchor=west
    },
    cardbold/.style={card, font=\fontsize{8}{10}\selectfont\sffamily\bfseries},
    sub/.style={
      font=\fontsize{7}{8}\selectfont\sffamily, text=tubMuted,
      align=left, anchor=west
    },
    flow/.style={
      ->, >=Stealth, draw=tubMuted2, line width=0.6pt
    },
  ]

  % --- Lane geometry. 4 group containers stacked side-by-side.
  \def\gh{4.2}     % group container height (cm)
  \def\gw{4.0}     % default group width (cm)
  \def\gwwide{5.2} % wider for Serve
  \def\xa{0}
  \def\xb{4.6}
  \def\xc{9.2}
  \def\xd{13.0}

  % ---------------- SOURCES ----------------
  \node[grp, minimum width=\gw cm] (gA) at (\xa, 0) {};
  \node[grphead] at ($(gA.north west) + (3pt, 2pt)$) {SOURCES};

  \node[card, text width=\gw cm - 8pt]
    (s1) at ($(gA.north west) + (4pt, -8pt)$)
    {RCSB PDB \\ \textcolor{tubMuted}{\fontsize{7}{8}\selectfont InterPro IPR000217}};
  \node[card, text width=\gw cm - 8pt, anchor=north west]
    (s2) at ($(s1.south west) + (0, -4pt)$)
    {TubulinDB \\ \textcolor{tubMuted}{\fontsize{7}{8}\selectfont Abbaali \emph{et al.} 2023}};
  \node[card, text width=\gw cm - 8pt, anchor=north west]
    (s3) at ($(s2.south west) + (0, -4pt)$)
    {NCBI taxonomy \\ \textcolor{tubMuted}{\fontsize{7}{8}\selectfont ete3 lineages}};

  % ---------------- PROCESS ----------------
  \node[grp, minimum width=\gw cm] (gB) at (\xb, 0) {};
  \node[grphead] at ($(gB.north west) + (3pt, 2pt)$) {PROCESS};

  \node[card, text width=\gw cm - 8pt]
    (p1) at ($(gB.north west) + (4pt, -8pt)$)
    {HMM classify \\ \textcolor{tubMuted}{\fontsize{7}{8}\selectfont $\alpha,\beta,\gamma,\delta,\varepsilon$ + MAPs}};
  \node[card, text width=\gw cm - 8pt, anchor=north west]
    (p2) at ($(p1.south west) + (0, -4pt)$)
    {Isotype call \\ \textcolor{tubMuted}{\fontsize{7}{8}\selectfont UniProt $\to$ HGNC}};
  \node[card, text width=\gw cm - 8pt, anchor=north west]
    (p3) at ($(p2.south west) + (0, -4pt)$)
    {MUSCLE align \\ \textcolor{tubMuted}{\fontsize{7}{8}\selectfont per-family master MSA}};

  % ---------------- STORE ----------------
  \node[grp, minimum width=3.4cm] (gC) at (\xc, 0) {};
  \node[grphead] at ($(gC.north west) + (3pt, 2pt)$) {STORE};

  \node[cardbold, text width=3.4cm - 8pt, anchor=north west,
        draw=tubAccent, fill=tubAccentSoft]
    (st1) at ($(gC.north west) + (4pt, -8pt)$)
    {Neo4j graph};
  \node[sub, text width=3.4cm - 8pt, anchor=north west]
    at ($(st1.south west) + (0, -3pt)$)
    {Structure, Entity,\\ Variant, Modification,\\ Chemical};
  \node[sub, text width=3.4cm - 8pt, anchor=south west, text=tubMuted]
    at ($(gC.south west) + (4pt, 6pt)$)
    {join key:\\ \texttt{\fontsize{7}{8}\selectfont Variant.master\_index}};

  % ---------------- SERVE ----------------
  \node[grp, minimum width=\gwwide cm] (gD) at (\xd, 0) {};
  \node[grphead] at ($(gD.north west) + (3pt, 2pt)$) {SERVE};

  \node[card, text width=\gwwide cm - 8pt]
    (sv1) at ($(gD.north west) + (4pt, -8pt)$)
    {FastAPI \\ \textcolor{tubMuted}{\fontsize{7}{8}\selectfont /structures /polymers /ligands /msa}};
  \node[card, text width=\gwwide cm - 8pt, anchor=north west]
    (sv2) at ($(sv1.south west) + (0, -4pt)$)
    {Next.js client \\ \textcolor{tubMuted}{\fontsize{7}{8}\selectfont Mol* + Nightingale}};
  \node[card, text width=\gwwide cm - 8pt, anchor=north west,
        draw=tubAccent, fill=tubAccentSoft]
    (sv3) at ($(sv2.south west) + (0, -4pt)$)
    {LLM chatbot \\ \textcolor{tubMuted}{\fontsize{7}{8}\selectfont natural-language $\to$ Cypher}};

  % ---------------- Flow arrows ----------------
  \draw[flow] (gA.east) -- (gB.west);
  \draw[flow] (gB.east) -- (gC.west);
  \draw[flow] (gC.east) -- (gD.west);

\end{tikzpicture}
